{
  "name": "svg-bulk-generater",
  "version": "1.0.1",
  "description": "Auto-generate stock-ready SVG animation batches from a single topic.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
